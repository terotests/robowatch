
; simple fs observer and shell script runner
watch src/**.ts {
  shell {
    "cd src && tsc"
  }
}

; testing ts2redux compiler
watch src/redux/**.ts {
  shell {
    "ts2redux src/redux"
    'echo "OK, looks good so far"'
  }
}
